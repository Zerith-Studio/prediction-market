# PitchMarket — Progress Log

**This file is the single source of truth for "where are we right now."**
`PROJECT_PLAN.md` says what we're building and why. `docs/interface-contract.md` is the
E1↔E2 boundary. **This file says what actually works today.**

> **Rule for both engineers:** if you change code, change this file in the same commit.
> Update the component table, tick the checklist, and add a Changelog row. A claim in this
> file means *"I ran it and saw it work"* — not *"I wrote it and it should work."*
> If you didn't verify it, mark it 🟡 and say what's unverified.

Legend: ✅ done & verified · 🟡 written but unverified · 🔴 not started / blocked

---

## 1. Status at a glance — 2026-07-12 (Day 8 of 11)

| | |
|---|---|
| Deadline | **2026-07-15** (internal) · judged by 2026-07-29 |
| Days left | **3** |
| E1 program | ✅ deployed on devnet; full lifecycle proven |
| E2 backend | ✅ **ALL REAL DATA**: TxLINE live feed (free-tier on-chain subscription — no email needed), markets auto-created on devnet, UI trades settled by the crank on devnet, real two-step deposits |
| Frontend | ✅ live-only (fixtures deleted): markets index, market page, **combos builder**, **precision pools**, portfolio with exit/cancel/realized+unrealized PnL (BBP mark) |
| **Remaining** | deploy/hosting, demo recording, human browser click-through |

**Honest summary.** Both halves of the trustless floor now work — separately. E1: the
§4 toolchain blocker is fixed, the program compiles to BPF, and the full lifecycle
(`initialize_market → deposit → settle_match (NORMAL/MINT/MERGE) → cancel fail-closed →
resolve_market → redeem`) runs green on `solana-test-validator` with balance assertions;
`sig_verify` executed for real. E2: the whole backend (matching, Postgres mirror,
crank builder, API/WS, RFQ, precision, bot, lifecycle) passes an HTTP end-to-end suite
against real Postgres. **Not yet done: the two halves have never met on devnet.** The
crank still settles in off-chain mirror mode, and E1's tx-size finding means the Go
crank needs a v0 + Address Lookup Table rework before it can submit for real. That
join — Go crank → deployed program on devnet — is the last hard step of the floor.

---

## 2. E1 — Anchor program (`programs/pitchmarket`)

Builds to BPF (`cargo build-sbf`, see §4). ✅ marks below = **exercised on a local
validator** via `tests/` (`npm test`), 8/8 passing. Not yet run on devnet.

| Instruction | State | Notes |
|---|---|---|
| `initialize_market` | ✅ | Market PDA + 2 outcome mints + pool. localnet |
| `init_vault` / `deposit` | ✅ | Vault PDA custody; USDC moved into vault ATA. localnet |
| `settle_match` NORMAL | ✅ | peer-to-peer USDC↔shares swap. localnet |
| `settle_match` MINT | ✅ | opposite-outcome buys mint a complete set into the pool. localnet |
| `settle_match` MERGE | ✅ | opposite-outcome sells burn a complete set, release pooled collateral. localnet |
| `cancel_order` | ✅ | maker cancels; a later settle of that order fails closed (`OrderClosed`). localnet |
| `resolve_market` | ✅ | **tier-a only** (operator-signed); localnet. Tiers b/d not started |
| `redeem` | ✅ | burns winning shares, pays 1:1 from pool. localnet |
| `sig_verify::verify_order_signature` | ✅ | ed25519 sysvar introspection **executed for real** in settle_match. TS borsh == `sig_verify.rs::borsh_order` proven at runtime; Go borsh pinned by golden vectors (§3) |
| `combo_accept` | 🔴 | typed stub |
| `resolve_combo` | 🔴 | typed stub |
| VOID path | 🔴 | |
| Oracle tier b (challenge) / d (TxODDS sig) | 🔴 | gated on TxODDS reply |

**Two program changes were needed to build & run** (PR #3):
- `SettleMatch` accounts are now `Box`ed — the context otherwise overflowed the 4KB BPF
  stack frame by 64 bytes (only surfaces at BPF build, not `cargo check`). ABI-unchanged:
  account order, args, and semantics are identical, so the Go crank needs no change here.
- `Cargo.toml` gained the `idl-build` feature (was missing; blocked IDL generation).

**⚠️ Cross-track finding:** the settle_match tx (2 ed25519 precompiles + the
`settle_match` ix) is **1453 bytes > the 1232 legacy limit**. It only fits as a **v0 tx
with an Address Lookup Table** — `tests/lifecycle.ts` shows how. **The Go crank
(`crank.TxBuilder`/`RPCSubmitter`) currently builds a legacy tx and must be reworked
before devnet settlement** — tracked in §5/§7.

**Program ID** `3fdgRPcZnwWcaGi197dkZDyq24VHoWJcGzKTVfMxNPWs` — pinned in `declare_id!`
and `Anchor.toml`.

⚠️ **The keypair at `target/deploy/pitchmarket-keypair.json` is gitignored and exists on
one machine only.** Both engineers can *build* this program ID, but only whoever holds
that file can *deploy* to it. **Decide before deploy day:** `git add -f` it (fine for a
devnet hackathon) or share out of band. If it's lost, the program ID changes everywhere.

---

## 3. E2 — Go backend (`backend/`) — completed 2026-07-11

Verified with `go build ./... && go vet ./... && go test -p 1 -count=1 ./...` against a
real Postgres (Neon; each test run creates and drops a scratch database).

| Package | State | Verified how |
|---|---|---|
| `matching` | ✅ | Unified ladder: NORMAL + **MINT** (two BUYs ≥100) + **MERGE** (two SELLs ≤100), price-time priority across both populations, cancel, expiry, replay-reject, revert `Unfill`, book snapshots. 15 unit tests. |
| `models` | ✅ | borsh + sha256 hashing, ed25519 sign/verify, fee/cost formulas, base58/hex wire helpers. **Golden vectors pinned Go↔Rust** (`hash_conformance_test.go` ↔ `sig_verify.rs` tests) — with E1's runtime TS↔Rust proof, all three encoders are now cross-checked. |
| `store` | ✅ | Full Postgres layer: orders + soft-locks (BUY locks USDC, SELL locks tokens — no naked shorts), fills mirroring **lib.rs money movement exactly** (NORMAL at fill price, MINT/MERGE at own limits), RevertFill, balances, positions, combos, precision, oneliners. Integration-tested against Neon. |
| `exchange` | ✅ | The trading core: sig verify → soft-lock → match → mirror → crank → WS. Both API and bot submit through it. Revert→reconcile (§6.2 IC) proven end-to-end. |
| `crank` | 🟡→rework | Builds the §6.5 3-instruction tx byte-verified against sig_verify.rs checks; RPC submitter written. **BUT: emits a LEGACY tx — E1's §2 finding (1453 B > 1232) means it must be reworked to a v0 tx + Address Lookup Table before devnet.** Off-chain mirror mode is the default until then. |
| `ws` | ✅ | Hub with the six pinned events; slow-client drop; tested with live WS clients. |
| `api` | ✅ | Full REST surface + `/ws`. **End-to-end suite over real HTTP**: deposit → signed orders → MINT fill → crank capture → WS events → Postgres rows → portfolio → resolve → settlement endpoint. Bad sig → 401, replay → 409, double-accept → 409, post-kickoff precision → 410. Admin now also exposes definition-backed custom market creation (`/admin/market-definitions`, `/admin/markets/custom`) and records resolution evidence. |
| `rfq` | ✅ | Combo lifecycle with **mutex groups**, sig + expiry + leg-match checks, single-use accept, resolve sweep reading the same market outcomes binary settlement writes (ADR 0004 seam). On-chain `combo_accept` behind an interface (Noop until E1 lands it). |
| `precision` | ✅ | Kickoff-lock, one-entry-per-wallet, σ-normalized score k=2, pool payout ±3 micro dust, rake, VOID refunds. ADR 0006 end-to-end. |
| `mmbot` | ✅ | Two-sided **MINT liquidity** (both quotes are BUYs — bot needs only USDC), re-quote on fair ticks, RFQ quoting per the §5 formula, exposure cap, crowd-seeding distinct persona wallets. |
| `lifecycle` | ✅ | Fixture → 7 template markets → replay feed → full-time resolves all templates correctly, settles precision, sweeps combos; balances verified to the micro. Abandoned match → VOID + refunds. |
| `feed` | ✅ | `replay` drives the lifecycle test; `txodds` SSE skeleton tested against a fake server — real endpoint shapes pending TxODDS reply. **Now also maps the scores feed's `lineups` (starting XI + subs) + rich stats (possession, cards, corners, period) → `EventLineup` / enriched `EventScore`; the live wiring is 🟡 (no in-play WC match to confirm against — the replay path is ✅).** |
| `oneliner` | ✅ | Claude Messages API client behind a Generator seam; 2-min ticker; tested with fake. Real API 🟡 (needs key at runtime). |
| `index` | ✅ | OrderStatus mirror processor (chain wins) tested with fake source; RPC poller written 🟡 (needs deployed program). |
| `cmd/server` | ✅ | Full wiring, env config (+ .env), graceful shutdown, `DEMO_FIXTURE` demo mode, CORS for browser clients. See `.env.example`. |
| `frontend/` | ✅ | Next.js app (E1's build) + motion-polish pass + **wired to the live exchange**: REST mapping (unified YES ladder from the outcome-indexed book), `/ws` stream (book/fill/oneliner/match_state), Privy embedded wallet behind `NEXT_PUBLIC_PRIVY_APP_ID` with a localStorage ed25519 demo wallet fallback, real order signing (borsh golden vector pinned as a 4th encoder, gated in `prebuild`). Browser-flow e2e green vs the real backend (fund → sign → 401/409 semantics → ladder → portfolio). Admin has fixture lifecycle controls plus the new market builder for fixture/competition/team/player/custom markets; Golden Boot/Golden Glove are manual-required until TxLINE player/award endpoints are confirmed. Fixture mode still works with zero infra. Team imagery: local flag SVGs (`public/flags/`, flagcdn public domain) on match headers, market/pool cards, MatchHero, combos, precision — `lib/teams.ts` map + gradient-dot fallback for unknown teams. |
| `mobile/` | 🟡 lib e2e ✅, UI unverified | Expo core loop: markets, market+trade, deposit, portfolio; Privy behind `EXPO_PUBLIC_PRIVY_APP_ID` + SecureStore demo wallet default; borsh vector gated (5th encoder, TS↔Rust↔Go↔web↔mobile). **`mobile/scripts/e2e-flow.ts` (`npm run e2e`) ran GREEN 3/3 vs a live `cmd/server`** (see §9 Jul-16 row for exact setup — no Neon `.env` on this machine, so this ran off-chain-mirror mode against a local throwaway Postgres container instead of Neon; same Go store/exchange code path either way). **Caught and fixed a real bug while doing it**: `randomSalt()` produced a 63-bit value, but `api.postOrder` wire-encodes salt via `Number(salt)` — JS silently rounds anything ≥2^53, so the signed bytes and the wire salt diverged and the backend 401'd on almost every order (~99.9% of draws). Fixed by narrowing `randomSalt()`'s range to <2^53 so the bigint→Number→JSON→uint64 round-trip is exact; added a regression test. **The identical bug exists in `frontend/lib/borsh.ts`'s `randomSalt()`/`TradePanel.place()`, untouched here (out of scope — mobile/ and progress.md only); flagging for a follow-up fix.** All 8 on-device click-through flows (Step 3 of the brief) are **unrun — no physical device available in this environment** — every UI screen (markets, market+ladder, trade, deposit, portfolio) stays 🟡 written-but-unverified-on-device until a human does that pass. |

**Test-suite caveats:** DB-backed tests are slow (~300ms RTT to Neon per statement) and
**network-flaky**: parallel packages contend on the shared endpoint, and even serialized
runs occasionally hit Neon DNS/TLS timeouts mid-suite — a failed package showing
`dial error`/`no such host` is the network, not the code; re-run it. Use
`go test -p 1 ./...`. Knowns: pgx simple-protocol needs explicit `::bigint` casts and
JSONB params as strings; `uint64` salts scan via `int64`.

---

## 4. ✅ RESOLVED — program now builds to BPF (fixed 2026-07-12, PR #3)

The `edition2024` failure was caused entirely by an **old platform-tools** (v1.43 →
rustc/cargo 1.79), which can't parse deps that Anchor 0.31.1 pulls. The fix is a
**modern Agave install** (platform-tools **v1.54** / rustc 1.89).

**How it was fixed (reproducible on a fresh machine):**
1. Install Rust (`rustup`), Agave CLI 4.1.1 (`release.anza.xyz/stable/install` →
   platform-tools **v1.54**), and Anchor via avm.
2. **Build with `cargo build-sbf` from the program dir, NOT `anchor build`.** The crux:
   `anchor build` (and `anchor idl build`) runs a toolchain override that **re-installs
   Solana 2.1.0 and repoints `active_release` back to the old v1.43 tools** — re-breaking
   the build (this is the "inconsistent state" the 07-10 note hit). After any `anchor`
   invocation, repoint:
   ```sh
   cd ~/.local/share/solana/install
   ln -sfn "$PWD/releases/stable-<hash>/solana-release" active_release && hash -r
   cargo-build-sbf --version   # must read platform-tools v1.54 / rustc 1.89
   ```
3. `cd programs/pitchmarket && cargo build-sbf` → `target/deploy/pitchmarket.so` (419 KB).

**IDL:** `anchor idl build` chokes on the two `ostatus` PDAs whose seed is a function
call on an instruction arg (`sig_verify::order_hash(&taker)`). Workaround: temporarily
swap those seeds for a plain arg field to emit the IDL, then restore. The runtime `.so`
keeps the real hash-based seeds.

**Verify on a second machine** — fixed on one clean box; E2 should reproduce.

---

## 5. Definition of done for the floor (one match, one binary market, fully trustless)

- [x] program compiles to BPF — `cargo build-sbf` (§4); 419 KB `.so`
- [x] full lifecycle green **on localnet**: signed order → settle_match (all 3 paths) →
      cancel fail-closed → resolve_market → redeem, balances asserted (`npm test` 8/8)
- [x] borsh conformance: TS↔Rust proven at runtime; Go↔Rust pinned by golden vectors
- [x] crank builds the §6.5 3-instruction tx — TS reference proven on localnet; Go
      builder byte-verified in unit tests
- [x] **Go crank reworked to v0 tx + Address Lookup Table** — per-market cached LUT
      (`crank/lut.go`); size proven in tests: legacy 1421 B ❌ vs v0 1116 B ✅ (limit 1232)
- [x] program deploys to **devnet** at the pinned ID — deployed 2026-07-15, tx
      `5Ayf6cLmSpqFue5odVvTVSBQSPMyJjyV6ndhp9FPu6F46CYDSkJucuDyPTpKMQvbpfv4XzC33v4bnfnaj4xXgVqa`
- [x] one signed order → matched → **Go crank** settles on devnet — ed25519 verify +
      fill-accounting + MINT executed, tx
      `3zNVPQJqLZhAuRpEmCzGxVfA9aqQe3mm3qT1yFzcN34rrNqM1Eu2oyuagxvdcT51xTjW86ggzjNGhrbvYoKzvdXS`
- [x] `resolve_market` → `redeem` on devnet → user's USDC moved 1:1 — txs
      `5oNcWKQBin6atteQcvAAtEkdivE5q9hXKmYXWeNiKzrXrS7X2VJN2SvSe7pxQ8oCMvjrSjBMr2T9i1uWtVJfXiK8` /
      `4qKCYL4G1VzsPighcWLQ6wgEfYBggHnFCkpHfomXBkWdCfVzrEHv4Ju3dLAwtKHNx62WEyV7Tvi2VqxeRSMWkMku`

**THE FLOOR IS DONE.** `go run ./cmd/devnet-e2e` reproduces it end-to-end (all balance
assertions green: 60/40 vault debits, complete set minted, pool 100% collateralized,
1:1 redemption). Devnet RPC note: public endpoint rate-limits aggressive status polls —
the harness and crank poll gently (1.5–2.5s) with long windows; a tx that "times out"
usually landed (check `solana confirm`).

---

## 6. Open decisions

| # | Decision | Owner | Status |
|---|---|---|---|
| 1 | ~~Commit `pitchmarket-keypair.json` or share out of band?~~ | both | **CLOSED 2026-07-12: committed** (devnet-only key; `git add -f`, on `feat/devnet-settlement`) |
| 2 | Oracle tier for demo: a (operator) vs d (TxODDS signed) | E1 | open, gated on TxODDS reply |
| 3 | TxODDS signed-data email sent? | — | **still unknown — confirm** |
| 4 | ~~Postgres vs in-memory~~ | E2 | CLOSED 2026-07-11: Postgres (Neon), wired + tested |
| 5 | Combos on-chain (`combo_accept`) vs off-chain for demo | E1 | E2 ships either way (interface seam); default off-chain per cut plan |

---

## 7. Next actions

**Full handoff (runbooks, invariants, punch list with code seams): `docs/HANDOFF.md`.**

**E2:** **frontend** — the last unstarted scope, with the deadline TODAY. The backend
serves everything it needs (HANDOFF §5.1 is the API contract); wire
`SOLANA_RPC_URL`/`OPERATOR_KEYPAIR` env to run the server in on-chain mode against the
deployed program. Then HANDOFF §5.2 (on-chain market lifecycle wiring).

**E1:** `combo_accept` / `resolve_combo` → oracle tier d if TxODDS replies (cut-safe:
combos run off-chain behind the interface seam).

**Both:** confirm the TxODDS email (decision #3); record the demo.

---

## 8. Housekeeping / paper cuts

- Neon DATABASE_URL lives in the gitignored `.env` (`.env.example` documents the shape).
  Rotate the credential after the hackathon (it was shared in chat).
- **Toolchain trap:** any `anchor` CLI invocation may silently repoint `active_release`
  to old platform-tools — see §4 step 2 before debugging "mystery" build failures.
- `docs/interface-contract.md` §6.5-above-§6 ordering still unfixed; §6.5 should also
  gain the v0+ALT requirement once the Go crank lands it.
- DB tests: `go test -p 1` (see §3 caveats).

---

## 9. Changelog

Newest first. One row per meaningful change. **Append here in the same commit as the code.**

| Date | Who | What changed | Verified how |
|---|---|---|---|
| 2026-07-19 | Prasad | **docs-site cleanup — standalone technical reference.** Stripped all internal project-management framing from the Mintlify site so it reads as an external technical doc: removed every E1/E2 engineer-track label (rewritten to "the on-chain settlement program" / "the backend service"), the Superteam × TxODDS hackathon framing, day-count/deadline/status flavor, ✅/🟡/🔴 verification marks, and `progress.md`/`PROJECT_PLAN.md`/`HANDOFF.md` source-cruft references. Deleted `reference/status.mdx` (live status board — doesn't belong in a technical doc) and removed it from `docs.json` nav. Kept and sharpened the technical content: architecture, TxLINE oracle integration, matching engine (NORMAL/MINT/MERGE, soft-locks, crank, money-movement), program instructions/accounts/signed-order verification, REST+WS surface, setup/toolchain, ADR index. No `backend/`, `frontend/`, `programs/`, or `docs/` content touched. | `npx mint broken-links` → no broken links ✅ · `npx mint dev` boots, `GET /` → HTTP 200 ✅ · all 10 remaining nav pages resolve ✅ |
| 2026-07-19 | Prasad | **Settlement-tx link on closed markets + false-LIVE status fix (frontend).** (1) When a market is closed/settled, `TradePanel`'s locked state now surfaces the on-chain `resolve_market` tx (TxLINE/TxODDS-driven) as a `VerifyLink` to the Solana devnet explorer. Fetched lazily via `getSettlement` **only** when the market is not open; matches the *resolve* step specifically (not the always-present deploy tx) and renders nothing when no resolve sig exists yet. (2) Fixed the false "LIVE" badge: `useLiveMarket`'s `match_state` handler had **no fixture guard** (every other WS event guards on `market_id`) and **fabricated status from the event type** (`full_time ? finished : live`), so any non-full_time tick — a sparse `odds` re-broadcast, or an event from *another* fixture — flipped the viewed match to LIVE and held it. Extracted the reducer into a pure, tested `lib/matchState.ts` (`applyMatchState`): fixture-guarded, and status changes **only** on real lifecycle transitions (`kickoff`→live, `full_time`→ft), else preserved. Added self-updating polls (market page re-fetches the authoritative match, index re-fetches matches/markets, both 15 s) so `scheduled → live → finished` reflects on its own. **⚠️ Backend flag (not fixed here):** France v England is served by the running backend as `match.status:"live"` though the match is finished (all its markets are `settled`) — a terminal status regressed to live because `store.SetMatchState` isn't monotonic and a looping/stale replay re-emitted kickoff/score after full_time (the running binary also still broadcasts `match_state` for odds ticks, superseded by 281ed89). Recommended minimal backend fix: make `SetMatchState` never regress from `finished`. The frontend now faithfully reflects + self-updates from the authoritative status, so France corrects itself the moment the backend reports `finished`. | `npx tsc --noEmit` ✅ · `npm run build` (9 routes, borsh gate) ✅ · `lib/matchState.test.mts` 8/8 (`node --experimental-strip-types`) ✅ · browser vs live stack (localhost:3005→8080): Part 1 — settled market w/ resolve tx shows Verify link (real sig), settled market w/o tx + open market show none ✅ · Part 2 — scheduled *Spain* stays SCHEDULED over 70 s of live feed; injected odds/cross-fixture ticks no longer flip it ✅ |
| 2026-07-18 | Ashish | **Trustless Claim (user-signed on-chain redeem) + cancel-exit button.** The on-chain `redeem` requires the winner's own signature (`user: Signer`) — the operator can't submit it — so winners **claim** their real on-chain USDC. Backend: two-step cosigned `PrepareRedeem`/`CompleteRedeem` (mirrors the deposit flow) + `POST /wallet/redeem-init`/`redeem-complete`; `RedeemPosition` mirrors the confirmed redeem (credit balance + clear position). Resolution's off-chain auto-credit is now **mode-gated** — `lifecycle.autoRedeem` pays winners only in mirror mode (`Creator == nil`); on-chain leaves the position **claimable** (else it would double-pay the claim). Frontend: a **"claim $X"** button on won positions (redeem-init → wallet sign → redeem-complete → mirror), and a **"cancel exit"** button that one-click cancels the resting sell when a position is fully locked in an exit (replacing the disabled exit button). | `go build`/`vet` ✅ · new `TestRedeemPositionClaim` (per-user claim credits + clears + idempotent) + updated settle test ✅ · store + lifecycle + api suites ✅ · frontend `tsc` + `npm run build` (9 routes) ✅ |
| 2026-07-18 | Ashish | **Binary winners are auto-paid on resolution.** Previously `SettleMarket` recorded the outcome but never credited binary winners (only precision + combos auto-credited; binary payout was the on-chain `redeem`, which is user-signed and unwired in the live server). Now `SettleMarket` also runs `redeemBinaryWinners` in the same tx: winning-outcome holders are credited **$1 (1e6 micro) per share**, losing shares are worthless, VOID refunds each holder at `avg_cost`, and all positions on the market are cleared — the off-chain mirror of the on-chain 1:1 redeem, matching the precision/combo auto-payout pattern. Also **dropped the unimplemented "payout merkle root"** from ADR 0001 + glossary (the built system settles per-match on-chain + `redeem`, no merkle tree). | `go build`/`vet` ✅ · new `TestSettleRedeemsBinaryWinners` (YES holder +20×$1, NO holder nothing, positions cleared) ✅ · store + lifecycle + exchange (money-conservation) + api suites all green |
| 2026-07-18 | Ashish | **Option A: global/admin markets initialize on-chain (backfill + self-heal).** Trading a market that was never `initialize_market`'d on-chain (early global markets like "Golden Boot", created before the builder's on-chain init shipped) matched off-chain but every `settle_match` reverted (`create vault ATAs: IncorrectProgramId` — no outcome mints on-chain). New `reconcileMarketInit` (startup + 10-min ticker, on-chain mode) walks open binary markets and calls the **idempotent, cheap** `InitializeMarket` (GetAccountInfo first, tx only if missing) — backfilling the stuck markets and self-healing any failed init. The forward path (builder + `RegisterFixture`) already inits; this catches everything else. | `go build`/`vet` ✅ · InitializeMarket idempotency confirmed (serverchain.go: existence-checked) · **backfill verified on deploy** (see below) |
| 2026-07-18 | Ashish | **Position exit gates on un-locked shares (display bug).** A resting SELL exit locks the outcome tokens (no naked shorts), but the position UI used the **gross** holding as the exitable qty — so after one exit the position still showed "70 NO" with an active exit button, and a second exit failed `store: insufficient outcome tokens`. Backend already sent `yes_locked`/`no_locked`; the frontend was dropping them. Now `Position` carries the locks, `calcPosition`/`calc` expose `locked` + `available = qty − locked`, exit sells `available` (borsh + wire in lockstep), and the exit button disables at `available ≤ 0` ("all shares resting in an exit order — cancel to re-exit"); MarketPositions shows "· N in exit". | frontend `tsc` + `npm run build` (8 routes) ✅ · accounting confirmed correct (70 NO held, 70 locked by the resting sell) — display-only fix |
| 2026-07-18 | Ashish | **Resolution releases resting-order collateral.** `SettleMarket` only flipped status/outcome — resting orders on a settled/void market kept their soft-locked collateral forever. Now it runs in a tx that also **cancels every live order on the market and returns each maker's lock** (USDC for buys, tokens for sells) via new `cancelOpenOrdersForMarket` — atomic with settle, so it covers all binary resolution paths (fixture full-time, manual admin, half-time, abandoned/void). | `go build`/`vet` ✅ · new `TestSettleReleasesRestingOrders` (two makers' collateral fully returned on resolve) ✅ · full store + lifecycle suites ✅ |
| 2026-07-18 | Ashish | **13 Polymarket-style markets + half-time partial resolver.** New templates: double chance (1X/12/X2), Over 1.5 / 3.5, away DNB, both clean sheets, HT result (H/D/A), 1H Over 1.5, 1H BTTS (22/fixture, up from 9). Templates gained `ResolvesAt`; a new `feed.EventHalfTime` (emitted by txodds at `halftime_finalised`) drives `lifecycle.ResolveHalfTime`, so 1H markets settle at the break — with the full-time cascade as an idempotent fallback (no market can get stuck). | `go build`/`vet` ✅ · `TestBinaryOutcome` (every template's logic + a guard that all binary templates have a mapping) ✅ · `TestHalfTimeResolvesEarly` (HT settles, FT doesn't, FT cascade idempotent) ✅ · updated `TestScoreFolding` · lifecycle/mmbot/api/feed suites ✅ |
| 2026-07-18 | Prasad | **Related markets on the market page** — new "Related markets" strip on `/market/[id]` (after the book/trades, before comments), ranked same-fixture first then markets involving either team, capped at 6, computed client-side from the existing `/markets` + `/matches` (no backend change) via `lib/useRelatedMarkets.ts`. Extracted the index-page card shells (`BinaryCard`/`GlobalBinaryCard`/new `PrecisionCard` + styles) into shared `components/MarketCard.tsx` so the index and the strip render identical cards. Renders nothing when nothing is related. | `tsc --noEmit` ✅ · `npm run build` ✅ (`/market/[id]` compiles) · live browser check on Spain vs Argentina: strip shows the 6 sibling markets with flags + Yes/No buttons |
| 2026-07-18 | Codex | **Fix: global-market detail page no longer pretends Golden Boot/Glove are a match.** The prior non-fixture fix made global markets routable by synthesizing a fake match, but the page rendered it through `MatchHero`/`MatchCentre`, producing bogus UI like `player 0–0 Field` and "Live match stats". Market detail now branches: fixture markets keep the TxLINE match hero/centre; non-fixture competition/team/player/custom markets render a dedicated World Cup/global-market hero with scope, competition, subject, and resolution source. Synthetic fallback labels were also cleaned up for any internal consumers. | `npm run build` ✅ · direct Golden Boot route `/market/5df33989e03a65c8893f7125fedc63b179e12401237d3db256c04189e75e0d9c` returned 200 ✅ · frontend dev server restarted |
| 2026-07-18 | Codex | **Fix: non-fixture admin-created markets are now visible/tradable in web.** Root cause: custom World Cup / Golden Boot / Golden Glove markets correctly have no `match_id`, but the public web index only rendered markets grouped under `matches`, and `useLiveMarket` always called `api.getMatch(market.match_id)`. Added a "World Cup markets" section for competition/team/player/custom markets without a fixture, preserved new metadata in the public `Market` type, and made market detail synthesize a neutral competition context when `match_id` is empty. Also fixed the Market Builder template rendering so future `world_cup_winner` creates render `{{team}}` from `subject_id` (e.g. "France to win the World Cup") instead of persisting "to win the World Cup". Existing bad-title rows remain as-created unless edited/recreated. | `npm run build` ✅ · live dev server restarted cleanly · `GET /` 200 ✅ · direct global market route `/market/68cda2ed0d7ed38f564ed1b020b64a4efa5cbd70f47ef2d7a4b28edb272b3997` 200 ✅ |
| 2026-07-18 | Ashish | **Hotfix: `GET /markets` 500'd against prod (broke the whole markets page).** Two markets with a NULL `match_id` — "Golden Boot" / "Golden Glove", tournament-wide award markets manually inserted into the DB (no code path creates match-less markets) — made `scanMarket` fail `cannot scan NULL into *string`, and one bad row took down the entire list. `ListMarkets` now `WHERE match_id IS NOT NULL` (the match-centric UI can't render a match-less market, so exclude rather than 500), and `marketColumns` uses `COALESCE(match_id::text,'')` so any other market read (GetMarket etc.) survives a NULL too. Bad rows left in place (not mine to delete) — flagged for a decision: delete, or add real tournament-market support. | `go build`/`vet` ✅ · fixed query verified vs live Neon (18 markets, no error; was erroring on 2/20) · **deployed** — `GET /markets` → 200 (18 markets) local + public HTTPS with the Vercel origin, CORS reflected, error log empty |
| 2026-07-18 | Ashish | **Self-trade prevention (cancel-resting) + action toasts.** (1) **STP** — the matching engine never fills a taker against its OWN resting order (was fully allowed + free — fees are 0 — so a wallet could wash-trade and paint the tape, moving the mid/chart/breaking-news Yes%): `matching.cross` detects `bestMaker.Order.Maker == taker.Maker`, cancels that resting order (it leaves the book) and keeps matching against everyone else, returning the cancelled hashes. `exchange.SubmitOrder` releases each STP-cancelled order's soft-lock (same path as a user cancel) + WS `cancelled`/`reason:self_trade_prevention`, and the `POST /orders` response gains `self_trade_prevented:[hashes]` so the trader is told precisely. Precision (one entry/wallet) + combos (RFQ, maker≠taker) are unaffected — CLOB-only. (2) **Sonner toasts** — global `<Toaster>` + `lib/toast.ts` `notify` helper; TradePanel (place → fill/resting/self-trade, deposit) and `usePositionActions` (exit/cancel, used by market page + Portfolio) fire text-only, semantic-colored toasts (no icons/emoji). | `go build`/`vet` ✅ · **matching STP tests** (falls through to another maker; only-self-liquidity rests) ✅ · exchange invariant suite ✅ · mmbot suite ✅ (two-sided quotes leave a spread → don't self-cross) · frontend `tsc` + `npm run build` (8 routes) ✅ · **toast visuals not viewed** — Chrome extension not connected |
| 2026-07-18 | Prasad | **Watchlist feature (web) — per-wallet favourites.** Backend: new `watchlists` table (`wallet, market_id` PK, FK to `markets`, `created_at`) + store `AddWatch`/`RemoveWatch`/`Watchlist` (`internal/store/watchlist.go`); REST `GET /watchlist?wallet=`, `POST /watchlist`, `DELETE /watchlist/{market_id}?wallet=` (interface-contract §5 updated). Frontend: `api.ts` client methods, shared `WatchlistProvider` context (optimistic toggle + revert), `StarButton` (hidden when no wallet) on home cards + market-page header, a "Watchlist" group atop ⌘K search (de-duped, keyboard-nav integrity preserved), and a horizontal scroll-snap watchlist strip on the portfolio page under the Vault/P&L summary. Spec + plan in `docs/superpowers/`. | Store tests `go test -p 1 ./internal/store/ -run TestWatch` **PASS on live Neon** (round-trip ordering, idempotent add, remove, unknown-market→ErrNotFound) · `go build`/`go vet` clean · frontend `npx tsc --noEmit` clean on every task · **🟡 end-to-end browser smoke (favourite→search group→portfolio strip→reload persistence) and the API curl round-trip UNRUN — no live backend+web session this pass** |
| 2026-07-18 | Ashish | **Home screen mobile responsiveness.** (1) **Fixed the "Matches" ↔ eyebrow overlap** — that header row was `flex justify-between` with no gap, so on a narrow screen the long "TxLINE live data · settled on Solana devnet" eyebrow collided with the heading; now stacks (`flex-col`) on mobile, side-by-side (`sm:flex-row sm:justify-between`) on ≥640px. (2) **Hardened two market-card footers** (precision pool + resolved binary) with `min-w-0 truncate` + `gap-2` so a long kind label can't overlap the state badge. (3) **Mobile nav** — the primary nav (Markets/Combos/Portfolio) was `hidden md:flex` with no fallback, so phones had no way to navigate; added a `MobileNav` hamburger dropdown in `TopBar` (`md:hidden`, reuses the WalletMenu outside-click/Esc pattern), desktop untouched. | frontend `tsc --noEmit` ✅ · home route serves HTTP 200 · **not viewed at mobile width** — Chrome extension not connected; verified by responsive-CSS reasoning (stack/truncate/dropdown) |
| 2026-07-18 | Ashish | **User avatars (`@outpacelabs/avatars`) + comment author edit/delete + Enter-to-post.** (1) **Avatars** — `users.avatar_seed` column (defaults to the wallet in `UpsertUser`; idempotent `ALTER`+backfill), carried on every comment (`ListComments`/`InsertComment` `COALESCE(users.avatar_seed, c.wallet)`); new client-only `components/Avatar.tsx` wrapping the deterministic `GradientAvatar` (canvas kept off SSR via `next/dynamic {ssr:false}`), shown in the TopBar wallet button, on every comment, and in the admin mod list. (2) **Composer** — Enter posts, Shift/⌘/Ctrl+Enter for a newline, Esc cancels. (3) **Author self-service** — `POST /comments/{id}/edit` + `/comments/{id}/delete` (author-only → 403 otherwise), store `EditComment`/`DeleteOwnComment` + `edited_at` column + WS `edit` action; a `⋯` menu on your own comments (inline edit → "· edited"; delete). | `go build`/`vet` ✅ · store `TestComments` (now incl. edit/delete owner-checks) ✅ vs Neon · **live:** `avatar_seed` present on POST; edit **403 non-owner / 200 owner → `edited:true`**; self-delete **200** · frontend `tsc` + `npm run build` (8 routes, market 12.2 kB) ✅ · **caught+fixed:** `edited_at` needed an explicit `ALTER` (`CREATE TABLE IF NOT EXISTS` won't add a column to the already-existing `comments` table — first run 500'd) |
| 2026-07-18 | Ashish | **Per-market comments (threaded, likes, live, moderated).** New `comments` + `comment_likes` tables; `store/comments.go` (insert + reply-same-market check, list with like-count + viewer `liked`, like toggle, soft-delete, recent-count); a 7th WS event `comment`; `api/comments.go` — `GET`/`POST /markets/{id}/comments`, `POST /comments/{id}/like`, `DELETE /admin/comments/{id}` (operator-gated), with `MaxBytesReader`, empty/length(500)/URL/profanity guards + an in-memory per-wallet rate limiter (→ **429**). Frontend: `useComments` hook (REST + its own WS + optimistic + one-level reply nesting), `Comments.tsx` (wallet-gated composer, likes, replies, "[removed]"), mounted full-width on the market page; `AdminComments` moderation in `/admin`. **Auth: unsigned — the wallet is a client claim (spoofable), a deliberate product call; guards are length+rate+content only.** | `go build`/`vet` ✅ · store `TestComments` + api unit tests (limiter + content filter) ✅ · **live vs Neon:** post/reply/like/nested-get/empty-400/too-long-400/**429**/banned-400/bad-wallet-400/admin-**401** all ✓ · frontend `tsc` + `npm run build` ✅ |
| 2026-07-18 | Ashish | **Admin market pinning + hourly Exa-grounded Breaking News.** (1) **Pin** — nullable `markets.featured_rank` (idempotent `ALTER`; `ListMarkets ORDER BY featured_rank NULLS LAST`), `SetMarketFeatured`, `POST /admin/markets/{id}/pin` (operator-gated), `featured_rank` in the market DTO; a `★ pin` toggle in `/admin`. (2) **Breaking News** — new `internal/breakingnews` (Exa search client + optional Gemini condenser + hourly `Service` with a relevance guard, representative-market pick, and real Yes%/delta from `OddsSnapshot`), `breaking_news` table, `GET /news`, wired in `main.go` behind `EXA_API_KEY`. Frontend: `FeaturedHero` (pinned markets, paginated, live price + sparkline + "Latest:" blurb) + `BreakingNews` panel (market + real headline + Yes%/delta), stark-dark two-column index. **Grounded in real Exa articles + real odds — nothing fabricated; a match with no relevant fresh article gets no row.** | `go build`/`vet` ✅ · breakingnews unit tests (fake-Exa parse, relevance guard, delta) + `breaking_news` store round-trip ✅ · **live vs real TxLINE + Exa:** `/news` returned 2 real Sports Mole WC-final previews (real URLs/dates, Gemini-grounded summaries, France **67%** from real odds); pin → 401 unauth ✓ · frontend `tsc` + `npm run build` ✅ · `EXA_API_KEY` added to gitignored `.env` |
| 2026-07-18 | Ashish | **Market-level portfolio on the market page + combos panel restyle.** (1) **Market-level portfolio** — new `components/MarketPositions.tsx` renders the connected wallet's position + open orders for the CURRENT market, with inline **exit** (sell at best bid) and **cancel**, so you no longer have to open `/portfolio` to manage a trade. Extracted the exit/cancel signing into a shared `lib/usePositionActions.ts` (`exit`/`cancel`/`busy`/`error` + `calcPosition`) — byte-identical to the portfolio page's path (borsh SELL at BBP / `api.cancelOrder`). Mounted below the TradePanel in the market page's right column (sticky), self-fetches `getPortfolio` filtered to `market_id`, polls 6s, and refetches instantly when a trade is placed (`posRefresh` bump on `onPlaced`); renders nothing when disconnected/empty. (2) **Combos "Your combo" panel** restyled to the flat-system card language like the market/precision panels: `border border-line` square card, eyebrow "Your combo" + `{n}-leg parlay` title, big 40px Stake input, neutral `bg-ink` CTAs (Request-quote + Accept-quote, were `bg-accent`). | frontend `tsc --noEmit` ✅ (whole app) · market + combos routes serve HTTP 200 · exit/cancel logic reused verbatim from the working portfolio page · **live visual/click-through not run** — Chrome extension not connected |
| 2026-07-18 | Ashish | **Fixed + deployed: index poller no longer spams the RPC (was burning Alchemy free-tier quota every 5s).** `internal/index/index.go` `RPCPoller` now **self-disables** when the provider rejects `getProgramAccounts` (new `unsupportedRPC` guard matches "not available"/"not supported"/`-32600`): `pollOnce` returns `stop=true`, the poll loop exits, logs one WARN instead of thousands — reconciliation never worked on the free tier anyway and settlement is independent of it. Added an explicit `INDEX_POLL=off` gate in `cmd/server/main.go` too. **Deployed to prod** (`root@159.89.161.148`, pm2 `pitchmarket-backend`): built the linux/amd64 binary from the deployed binary's *exact* base commit (`58dd8ed`, read via `go version -m` vcs.revision) + only the `index.go` patch — **none of the uncommitted match-centre WIP**; kept `.rollback`/`.working-*` backups. | `go build ./... ` + `go vet ./internal/index` ✅ · `go test ./internal/index` ✅ · post-deploy: `getProgramAccounts` log lines **thousands→1** (one-time self-disable message), boots **"ON-CHAIN mode"**, `healthz` 200, 0 errors on startup · rollback = `cp pitchmarket.rollback pitchmarket && pm2 restart`. **Note:** MM bot now visibly tight-loops `"mmbot: persona entry: market is not open"` (pre-existing, was masked by the poller spam) — separate issue, untouched. |
| 2026-07-18 | Ashish | **Droplet is ALREADY on-chain — the 9 stuck fills are pre-restart artifacts (ops finding, no code change).** SSH'd the prod droplet (`root@159.89.161.148`, pm2 `pitchmarket-backend` on `:8090` behind nginx, config in `/root/pitchmarket/.env`, operator key `operator.json` present). It boots **"ON-CHAIN mode"** (`SOLANA_RPC_URL` + `OPERATOR_KEYPAIR` + `USDC_MINT=ymdid…` — note the droplet's mint differs from this laptop's `.pitchmarket-usdc=AELa…`) and **settles**: 1 confirmed / 0 reverted since the ~22:00 Jul-17 restart, verified real on devnet (`2wdAtN…`, slot 477005778). The 9 "settling…" fills are all **pre-restart** (≤21:57, mirror-era) — 0 new unsettled since 22:00 — so new trades settle fine; the old ones can't (takers have no on-chain vault, and the crank has no backlog resweep). **One live issue:** the `index` RPCPoller calls `getProgramAccounts` every 5s (`internal/index/index.go` `Every=5s`, hardcoded when on-chain, no env gate) — unsupported on Alchemy's free tier, so it errors + spams every 5s, burning quota that could rate-limit the crank. Fix needs a code gate + redeploy. | all droplet cmds read-only (recon/logs/`.env` redacted/`healthz` 200) — **no server change**; shared-DB check: unsettled=9, after-restart=0, settled-after-restart=1; devnet `getTransaction` on the post-restart settle ✓ |
| 2026-07-18 | Ashish | **Diagnosed the portfolio "settling…" fills — they are UNSETTLEABLE on-chain (not a bug).** "settling…" = a fill row with empty `settle_tx`. Scanned the live Neon DB: 9 fills stuck, all `MINT`, all created after a ~21:33 Jul-17 cutover where the deployed server dropped from on-chain mode → off-chain **mirror mode** (`OffchainSubmitter` returns an empty sig, so `onSettleConfirmed` writes `settle_tx=""` permanently — terminal, not transient). Built a read-only ops tool `backend/cmd/settle-backlog` (`-mode=check`) that reconstructs each fill from the `orders` table and probes its on-chain PDAs: **market ✓ / pool ✓ / makerVault ✓ (MM bot) / takerVault ✗** for all 9. Root cause: the human/demo **takers never funded an on-chain vault** — their deposits used the off-chain mirror faucet. And they cannot be funded by anyone else: `init_vault`/`deposit` require the vault owner's own `Signer` (`lib.rs`), and those demo-wallet keys live only in the users' browsers. So `settle_match` would revert (taker vault AccountNotInitialized); a crank re-run cannot fix them. Real fix = run the deployed server in on-chain mode + have takers do real on-chain deposits before trading. No product code changed. | read-only: `settle-backlog -mode=check` (no tx sent, no DB write) confirmed takerVault ✗ on all 9 · sample pre-cutover `settle_tx` verified as a real confirmed devnet tx (operator `2rRnd…` = fee payer) · `go build`/`vet ./cmd/settle-backlog` ✅ |
| 2026-07-18 | Ashish | **Precision entry panel → matches the market TradePanel design.** Restyled the `/precision/[id]` "Your entry" `<aside>` to the same flat-system card language: `border border-line` bordered card (square corners, `p-5 sm:p-6`, `lg:sticky`), `eyebrow` "Your entry" + market-title header, big 40px prominent **Your guess** / **Stake** inputs on `border-line2` hairlines (was 22px), **+1/+5/+10/MAX** stake quick-chips (nudge the existing `stake` state; MAX = balance-capped), and the neutral `bg-ink` primary CTA (was `bg-accent` green) — Connect/Entering/Enter-pool label unchanged. Widened the panel column 280→320px for the card. Entry logic (guess/stake/enterPrecision), leaderboard, and distribution untouched. | frontend `tsc --noEmit` ✅ · precision route serves HTTP 200 · no rounded corners / non-token colors in the file · visual review pending a human reload of `/precision/{id}` |
| 2026-07-18 | Codex | **Admin market-builder flow implemented.** Added definition-backed custom markets: `markets.match_id` is now nullable for non-fixture markets; `markets` carries `scope`, `competition_id`, `subject_type/id`, `resolution_source`, and machine-readable `rule_json`; `resolution_attempts` records admin evidence for every manual resolve. New `marketdefs` catalog exposes fixture stat, World Cup winner, team reaches final, group winner, Golden Boot, and Golden Glove templates; player/award markets are deliberately `manual_required` until TxLINE player leaderboard / official-award endpoints are confirmed. Backend admin routes: `GET /admin/market-definitions`, `POST /admin/markets/custom`; custom fixture markets upsert/map their fixture, custom competition/player/team markets do not require a match row. Frontend `/admin` now has a Market Builder section consuming the definition catalog and posting explicit resolution-rule JSON. | TDD red→green: store custom competition market without fixture, custom fixture market mapping, resolution evidence logging; API custom market create+resolve with evidence, fixture custom mapping, definitions endpoint. `go test ./internal/store ./internal/api ./internal/marketdefs -count=1` ✅ · `npm run build` ✅ (first sandbox run failed only on Google Fonts DNS; rerun with network passed) |
| 2026-07-18 | Prasad | **⚠️ Cross-machine finding: devnet settlement is machine-bound.** Every `settle_match` on this machine failed `ConstraintAssociated` (0x7d9) on `taker_usdc_ata` and the crank reverted every fill (positions kept vanishing). Root cause proven by deriving ATAs with the crank's own code (`FindAssociatedTokenAddress`): the crank passed the correct ATA for OUR auto-created mint (`T333c…`, `backend/.pitchmarket-usdc`), but the on-chain market accounts store a DIFFERENT `usdc_mint` — the fixture market PDAs are deterministic and were first initialized by a server run with another machine's mint cache + operator (shared Neon DB, per-machine mint/operator). Our operator can't mint that token, so this machine can never settle those markets on-chain. **Fix for the team: commit/share ONE `USDC_MINT` + operator keypair (like the program keypair, decision #1) — auto-created per-machine mints are a trap.** Tonight's workaround: `.env` runs settlement in off-chain mirror mode (SOLANA_RPC_URL commented, OPERATOR_KEYPAIR kept — the TxLINE provider needs the wallet) with the REAL TxLINE feed still live; TxODDS activation 503'd twice (their side) and came up on retry 3. Also: new `mobile/scripts/counterparty-fill.ts` (fresh wallet deposits then crosses a target wallet's resting orders — MINT for BUYs, NORMAL for SELLs) to demo real fills; widget final layouts per user review (portfolio 2×2 with big money row; large widget flat-state shows Available/Locked/Open-orders strip + divider-separated order rows). | ATA derivation cross-check via throwaway `backend/cmd/atacheck` (Left=crank ATA under T333c ✓, Right=program-expected ≠ any T333c ATA ⇒ foreign mint) · mirror-mode restart: fills STICK (user holds 20 YES FHG @42 + 20 YES draw @55, 4 fills, 1 resting) · app relaunched: market-screen chart renders the real fills (screenshot) · widgets 🟡 pending user's home-screen look |
| 2026-07-18 | Prasad | **Widgets iterated after on-sim review + market-screen chart.** Widgets (user feedback on real pinned widgets): Portfolio medium redesigned — Available is now a large left-column figure, Value/Unrealised/Realised as a compact right stack; Active-market large now lists resting open orders ("waiting to fill") when flat and shows up-to-2 "other positions" rows under the chart when not (Api.swift: Order DTO grew market_id/outcome, new `OrderCalc`, `ActiveMarketData` carries the whole summary). App: new `PriceChart` (react-native-svg) on the market screen — seeded from `/fills`, extended by live WS mids, dashed last-price line, honest empty state pre-first-fill. Also this session: ran the whole stack REAL on the sim — killed a stale demo-fixture server; `cmd/server` now ON-CHAIN + TxLINE live (France–England, Spain–Argentina; 16 open markets; bot vault funded on devnet); `mobile/.env` → localhost + Privy commented out (OAuth still blocked on the Privy dashboard native-app allowlist, so no login popup — demo wallet signs real orders meanwhile). | rebuild `expo run:ios` **Build Succeeded 0 errors** (Swift + svg pod) · gates: `tsc` clean, jest 9/9, borsh ok · user-pinned widgets rendered REAL portfolio data on the sim home screen ($980.60 avail, 4 orders/$19.40 locked) ✅ · new widget layouts + chart 🟡 pending next on-sim look; chart empty-state correct (zero fills exist yet — resting orders haven't crossed) |
| 2026-07-18 | Prasad | **iOS home-screen widgets built** (branch `feat/ios-widgets`, spec + plan in `docs/superpowers/`): WidgetKit extension target `mobile/targets/widget/` via `@bacons/apple-targets@5` (survives the gitignored-`ios/` prebuild) — Portfolio widget (medium: available/value/unrealised/realised, open-orders + locked footer) and Active-market widget (large: biggest position, current vs entry price, PnL, fills price chart with dashed entry line). RN side: `src/lib/widgetBridge.ts` writes `{wallet, apiUrl}` to App Group `group.com.pitchmarket.app` + reloads widgets from a `WidgetSync` child in `_layout.tsx` (both wallet backends); `app.config.js` injects optional `EXPO_APPLE_TEAM_ID`. Swift providers fetch `/portfolio`, `/markets`, `/markets/{id}/fills` on a 15-min timeline (5-min retry on failure); read-only, no secrets shared. | `npx tsc --noEmit` clean · `npm test` 9/9 (2 new widgetBridge tests) · `check-borsh` ok · `expo config --type prebuild` shows App Group + env team id · `expo prebuild -p ios --clean` ✅ · `expo run:ios` **Build Succeeded 0 errors**, `widget.appex` (`com.apple.widgetkit-extension`, iOS 17.5) embedded in the built app, installed on iPhone 17 simulator · 🟡 **unrun**: pinning the widgets on a home screen + data/deep-link check vs a live backend — needs a human (simulator/device) pass |
| 2026-07-18 | Prasad | **iOS home-screen widgets design spec** (`docs/superpowers/specs/2026-07-18-ios-widgets-design.md`): WidgetKit extension via `@bacons/apple-targets` (survives gitignored-`ios/` prebuild), portfolio widget (available/value/unrealised/realised/locked) + active-market widget (biggest position, sparkline from `/markets/{id}/fills`), App Group shares `{wallet, apiUrl}` written from `wallet.tsx`, ~15-min timeline refresh. No code yet. | doc only — n/a |
| 2026-07-18 | Prasad | **Frontend: wallet dropdown + search Esc fix.** (1) Clicking the top-bar address no longer logs you out instantly — it opens a `WalletMenu` dropdown (`frontend/components/TopBar.tsx`) showing the full public key, **Copy address** (transient "copied" confirmation via `navigator.clipboard`), and an explicit **Log out**. Closes on outside-click or Esc; keeps the accent dot + demo badge. Both actions go through the shared `usePitchWallet()` context (`address`/`isDemo`/`disconnect`), so behavior is identical on the Privy and demo backends — **Log out** calls exactly what the old click did (`wallet.disconnect`). (2) `CommandPalette` Esc now closes reliably: the handler was on the dialog `div`, so it did nothing while results loaded (input not yet focused) — added a global `window` keydown listener scoped to `open`, mirroring TopBar's ⌘K handler. Frontend-only. | `npm run build` ✅ (borsh golden-vector prebuild gate + all 8 routes) · **browser eyeball ✅** (demo wallet path, `npm run dev` :3003 — page renders, no compile/runtime errors) · 🟡 Privy path verified by shared-interface/code only, not executed (no `NEXT_PUBLIC_PRIVY_APP_ID`; Privy login blocked on dashboard allowlist) |
| 2026-07-18 | Ashish | **Real TxODDS match-detail data → frontend match centre.** Verified against the TxLINE OpenAPI (`docs.yaml`) exactly what the feed serves, then wired *only* what's genuinely TxODDS-native — **no fabricated data**. Backend: the scores feed's `lineups[]` (starting XI + subs: number, name, position, unit, starter, captain) and rich stats (possession, yellow/red, corners, period) were being **discarded** in `wireScore` — now decoded and normalized. New `feed.EventLineup`; enriched `EventScore` payload (`scorePayload` + `applyStats` now map the confirmed stat-key catalog 1–8 × period-prefix, P1/P2→home/away via `p1IsHome`); `statkeys.go`/`lineups.go` hold the dictionaries + normalization (formation *derived* from unit-line counts; position label only when a code is confirmed — the position-code table is PDF-gated, so unknown → number+name only, never a guessed label). Store: new `lineups` JSONB column (idempotent `ALTER`), `SetMatchLineups`, `GetMatchByID`, coalesced scans; `lifecycle.handleEvent` persists lineups + broadcasts via the existing `match_state` WS channel (`event:"lineup"` — no new WS type, surface stays frozen). API: `GET /matches/{id}` full detail. Frontend: `MatchCentre` (Match·Lineups tabs, sliding underline), `LiveStats` (possession bar + cards/corners), `Lineups` (unit-grouped formation columns + subs); `types.ts`/`api.ts`/`useLiveMarket` carry the new shapes and preserve sheets across score ticks. **Explicitly hidden (not in the feed): venue/capacity, referee, managers, standings/bracket, H2H history, form, season top-scorers, avg-goals-per-period.** | **Full pipeline verified end-to-end vs real Neon** (replay, `MM_BOT=off`): `GET /matches/{id}` returned status `live` + rich `live_state` (min 23, 1H, 1-0, possession 58/42, corners 3/1) + both sheets (Spain 4-3-3 / Argentina 4-4-2, 11 starters, Messi #10 captain), and persisted correctly post-FT (2-1, possession 55/45, corners 8/4). Store round-trip test `TestMatchLineupsAndLiveState` ✅ vs Neon (migration + `SetMatchLineups`/`SetMatchState`/`GetMatchByID` JSONB round-trip). `txodds` unit tests +2 (`TestLineupEmit` formation="4-3-3" & emit-once, `TestLiveStatsPayload` cards/corners/possession) ✅. `go build`/`vet` ✅ · frontend `tsc --noEmit` + `npm run build` (8 routes) ✅ · `/market/{id}` dev route compiles + serves 200. **Live TxLINE feed path 🟡** (no in-play WC match to confirm against) · **in-browser visual render not captured** — Chrome extension not connected this session |
| 2026-07-18 | Ashish | **Fix: every market page 404'd against the deployed backend.** The match-detail work repointed `api.getMatch` at a new dedicated route `GET /matches/{id}` (full `live_state` + team sheets), but the backend running at `159.89.161.148` predates that route and returns `404 page not found` — so `getMarket` (200) → `getMatch` (404) → `errorStatus: 404` → "Market not found" on **every** market. Made `getMatch` **fall back to the `GET /matches` list scan on 404**, so the page loads against both an old backend (basic match, no lineups/stats — `MatchCentre`/`LiveStats` already null-guard to an "awaiting stats" empty state) and the new one (full detail), and auto-upgrades once the droplet redeploys with the new route. Backend still needs the `/matches/{id}` redeploy to surface lineups live. | frontend `tsc --noEmit` ✅ · reproduced against the live API: `GET /markets/{id}` 200, `GET /matches/{id}` **404**, `GET /matches` list **contains** the match → fallback resolves → page loads · dedicated-route path unchanged when the backend has it |
| 2026-07-18 | Ashish | **TradePanel relayout (Polymarket-inspired), same content.** Kept every existing input/value/action (YES/NO select, Buy/Sell, limit price, size, cost/proceeds, max payout, submit + inline fund, footer note) and only restructured the layout into a self-contained card **that follows the flat design system** (square corners — the theme's `borderRadius.DEFAULT` is 0px — `border-line` hairline, no non-token fill, no decorative tile): header ("Selected" + market title) → Buy/Sell underline tabs → big filled **Yes/No pills** (selected = solid outcome colour, other = translucent tint of the same) → prominent **Size** number with **+100/+500/+1k/MAX** quick-add chips (chips just nudge the existing `size` state) → compact **Limit price** field → neutral **primary CTA** (`bg-ink`, reuses the existing keyed label/animation + Connect/Signing/Filled states) → **Shares / Cost** summary rows → big green **To win / Avg. price** payout footer. Market page: the trade-panel column dropped its `lg:rule-l lg:pl-10` since the panel is now a bordered card; the 2/3·1/3 chart↔panel split is unchanged. No trading logic, borsh, or API touched. | frontend `tsc --noEmit` ✅ clean (whole app, incl. the newly-wired MatchCentre) · market route serves HTTP 200, no compile errors · **live browser screenshot not taken** — Chrome extension not connected this session; visual review pending a human reload of `/market/{id}` |
| 2026-07-18 | Ashish | **Market page: chart survives reload, 2/3·1/3 layout, MatchHero live-label fix.** (1) **Chart persisted across reload** — `useLiveMarket` seeded the price chart with two flat points at the current mid, so a reload collapsed the graph to a flat line at the latest price (live it only moved because `book_update` appended points). Two-part fix: (a) a first-ever visit now reconstructs the series from the market's real `/markets/{id}/fills` (YES-cents price + ts, up to 100 newest-first), oldest→newest with the live mid pinned as the trailing point; (b) crucially, the backend stores **no mid-price time-series** and `/fills` only records *trades* — a resting order that shifts the YES mid (the common case: `midOf` = best bid when asks are empty) never produces a fill, so fills-seed alone still reloaded flat. So the hook now **persists the live series to `localStorage` per market** (`pm:chart:{id}`, capped 120) and restores it on reload (the user's own series wins, re-pinned to the current mid; fills seed only when nothing is stored). `priceDelta` measures from the first point. (2) **Layout** — chart and trade panel now sit side by side (chart `lg:col-span-2` ≈ 2/3, panel `lg:col-span-1` ≈ 1/3); order book + recent fills moved below full-width in a 2-col grid. (3) **MatchHero** — a live match with no clock/period rendered "LIVE · LIVE · undefined'" (`periodLabel` fell back to "LIVE", `minute` was undefined); the label is now built from only the parts present (`["LIVE", periodLabel(period), minute']`.filter(Boolean).join(" · ")), so it reads just "LIVE". | frontend `tsc --noEmit` ✅ · market route serves HTTP 200, no compile/runtime errors in dev log · full flow simulated against the exact helpers: first visit flat → place resting bid @45 (live `40→40→45`) → **reload preserves `40→40→45`, not flat**; reload with a server-side mid move appends the new point; fresh browser on the real 2-fill market `a99e3890…` reconstructs `50→34→50` · live browser screenshot **not taken** — Chrome extension not connected this session · **note:** local-only history (per browser), not a shared/cross-user series — a proper fix is a backend mid-price time-series + `/history` endpoint |
| 2026-07-18 | Ashish | **Market-maker desk polish + collateral guard.** (1) **Collateral guard** — `rfq.SubmitQuote` now rejects a `ComboQuote` the maker can't back: it checks the maker's available balance covers the quote's collateral (`payout − stake`) up front, so an unbacked quote can't sit on `/combos` only to fail the taker's accept through no fault of theirs (new `ErrInsufficientCollateral`). Best-effort — not a hard reservation across a maker's simultaneous quotes. (2) **`/mm` desk polish** — quick-pick payout multiples (2×/3×/5×/10×), live multiple + explicit risk framing ("you post $X collateral · win the $stake if any leg misses"), balance-aware blocking (mirrors the backend guard client-side), and enter/exit + signing motion. (3) Dev server default port 3002→3003. | `go build`/`vet` ✅ · frontend `tsc --noEmit` ✅ · borsh golden vectors unaffected · guard checked against `AcceptQuote`'s debit path (`payout − stake` into escrow); `/mm` exercised live earlier (MM wallet submitted backed quotes, all expired unaccepted, nothing mislocked) |
| 2026-07-18 | Prasad | **Docs site (`docs-site/`, Mintlify)** — new top-level Mintlify site (docs.json + 11 MDX pages): overview, architecture (E1/E2/frontend/mobile map + mermaid diagram), setup/toolchain (§4 platform-tools trap incl.), matching-engine concepts (NORMAL/MINT/MERGE, CTF solvency), trust model (ADR 0003/0005 story, honest about tier-a + off-chain combos), on-chain program reference (accounts/instructions/errors from lib.rs, stubs marked 🔴), signed-message + §6.5 v0/ALT tx layout, full REST route table (from `api.go Routes()`) + WS events, summarized ADR index, and a status page that defers to this file. Existing `docs/` untouched — the site links to it as source of truth. Run: `cd docs-site && npx mint dev` (see docs-site/README.md). | `npx mint broken-links` ✅ "no broken links found" · `npx mint dev` served locally, all 11 pages curl 200 with content, no errors in dev log · not deployed anywhere (local-only) |
| 2026-07-18 | Prasad | **Frontend: market images + team flags (Polymarket-style).** Polymarket's own CDN/API resets connections from this network, so per the task's fallback used equivalent official team imagery: 24 public-domain country-flag SVGs downloaded from flagcdn.com into `frontend/public/flags/` (committed locally — no hotlinking). New `frontend/lib/teams.ts` (team name → flag path, `flagSrc()`, plus the old `TEAM_DOT` gradients as fallback) and `frontend/components/TeamFlag.tsx` (`TeamFlag` circular flag img with gradient-dot fallback for unknown teams; `FlagPair` overlapping pair — the Polymarket match-tile look). Wired in: markets index (`app/page.tsx` — section headers + every binary/pool card, keyed by the fixture's home/away from `mapMatch`; `BinaryCard` gained a `match` prop), `MatchHero.tsx` (flags replace the old dots, local `TEAM_DOT` deleted), combos leg-picker headers, precision page subtitle. `lib/api.ts`/`borsh.ts` untouched. | `npx tsc --noEmit` clean · `npm run build` ✅ (borsh golden-vector prebuild gate + all 7 routes) · **browser eyeball ✅**: demo backend (`DEMO_FIXTURE=demo-final`, throwaway local Postgres container — no root `.env` on this machine, same substitution as the Jul-16 mobile row) + `npm run dev`, Playwright screenshots of index / market page / combos — Argentina🇦🇷/France🇫🇷 flag pairs on section headers + all 9 cards, circular flags in MatchHero, combos picker header; container + servers torn down after |
| 2026-07-18 | Prasad | **Frontend: ⌘K command-palette market search** — new `frontend/components/CommandPalette.tsx`, mounted from `TopBar` (so it's on every page): Cmd/Ctrl+K toggles it, plus a visible Search button (icon + `⌘ K`/`Ctrl K` hint) in the top bar. On open it fetches a fresh `listMarkets`+`listMatches` snapshot and filters client-side — case-insensitive every-word substring match across market title, rule, template kind label, type, and the joined match's home/away team names. Results grouped **Markets** (binary) / **Precision**; binary rows navigate to `/market/[id]`, precision rows to `/precision/[id]` (their actual page). Full keyboard nav (↑/↓ + Enter, Esc closes, mouse hover tracks selection, selected row scrolls into view), loading skeleton, empty state, not-configured state; existing dark tokens throughout (bg/line/dim/accent, eyebrow group headers, sharp radii). Extracted the index page's `KIND` template-label map to `frontend/lib/kinds.ts`, now shared by `app/page.tsx` and the palette. Frontend-only — no backend/program changes. | `npx tsc --noEmit` ✅ · `npm run build` ✅ (borsh golden-vector prebuild gate + all 8 routes) · 🟡 human browser click-through (hotkey on mac/win, live-data search) not run |
| 2026-07-18 | Prasad | **Fix Vercel frontend deploy** — `npm install` failed ERESOLVE: `@solana-program/memo@^0.8.0` (peer `@solana/kit@^3.0`) conflicted with kit 5.5.1 forced by Privy's dep tree; the local lockfile had shipped that inconsistent pair, which Vercel's strict peer check rejects. memo is unused in our code (only fills Privy's optional peer `>=0.8.0`), so bumped it to `^0.10.0` (peer kit `^5.0`) and regenerated `frontend/package-lock.json`. | `npm install --strict-peer-deps` ✅ clean · `npm run build` ✅ (golden-vector prebuild gate + all 8 routes) · Vercel redeploy 🟡 pending push |
| 2026-07-17 | Ashish | **Market-maker flows + portfolio hierarchy redesign.** (1) **Bot unblock** — admin `POST /admin/markets/{id}/price` pushes a fair price to the MM bot (`WithPricer`), which then quotes that binary market two-sided (liquidity to trade against + combo answers); `/admin` gains a per-market `¢`+quote control. (2) **Human market maker** — `GET /combos` lists open RFQs; new `/mm` page lets any wallet sign a `ComboQuote` and submit it (byte-exact `borshComboQuote`, pinned by `TestBorshComboQuoteGoldenVector` + `check-borsh.mjs`). (3) **Portfolio redesign** — flat 4-stat summary → two-number hero (Vault + Total P&L); empty sections hidden; section counts; History demoted to a muted secondary. (4) **Combo detail** — `/portfolio` combos now carry `leg_details`; the combo row shows each leg (YES/NO + title) + stake→pays. | `go build`/`vet` ✅ · frontend `tsc` ✅ · both borsh golden vectors ✅ (order + combo-quote) · verified live on the clean-DB on-chain backend: bot quoted a 2-leg combo ($5→$53.67) that was accepted; `/portfolio` returns combo `leg_details` |
| 2026-07-17 | Ashish | **Portfolio: precision + combo results, and two bug fixes found during live manual testing.** (1) **Order signature fix** — `randomSalt` produced a 63-bit salt signed over borsh but posted as `Number(salt)` (float64), which rounds above 2^53 → the backend rebuilt a different salt → every real UI order rejected as "Signature rejected". Capped the salt at 53 bits (lossless JSON round-trip). (2) **Precision page crash** — an empty pool returns `{entries:null}` (Go nil slice → JSON null); `entries.reduce` threw. Coerce `?? []` in `api.leaderboard`. (3) **Portfolio now shows precision + combo results**: `store.PrecisionEntriesForWallet`, enriched `EscrowRow` (stake/payout/legs), `/portfolio` returns `precision[]` + enriched `combos[]`; portfolio page renders Precision pools + Combos sections (guess/stake/score/payout/PnL; legs/stake/status/payout/resolve-tx). Added `docs/MANUAL-TEST-PLAN.md`. | `go build`/`vet` ✅ · frontend `tsc` ✅ · borsh golden vector still ✅ · verified live against the clean-DB on-chain backend: `/portfolio` returns a settled precision entry (guess 3, score 1.0, paid $3.38) + enriched combos |
| 2026-07-17 | Prasad | **Mobile: dev-client build for Privy** — `npx expo prebuild`/`run:ios` built the native iOS dev client (`ios/` gitignored; package.json ios/android scripts now `expo run:*`). App boots natively with Privy active; Privy login currently blocked on a dashboard setting (`com.pitchmarket.app` must be allowlisted as a native app identifier) — error pinned in Metro logs. | dev client built + installed on iPhone 17 simulator, our bundle loads (markets live); Privy OAuth 🔴 pending dashboard allowlist |
| 2026-07-16 | Prasad | **Mobile: first native (simulator) boot** — two real native-bundle fixes: (1) Privy's transitive dep `jose` resolves to its Node build on native (no `react-native` export condition) and crashed the bundle on `require("buffer")` — added `buffer` dep + `resolver.unstable_conditionNames = ["browser","require","react-native"]` in `metro.config.js` per Privy's RN setup docs; (2) web preview crash fixed earlier the same day (seedStore shim). | iOS bundle builds (3359 modules) · app boots in the iPhone 17 Pro simulator via Expo Go: markets index renders live data from the local demo backend (screenshot) · manual click-through in progress |
| 2026-07-16 | Prasad | **Mobile: web-preview fix** — `expo-secure-store` has no web implementation, so the demo wallet's restore effect crashed `expo start` web preview at mount (`getValueWithKeyAsync is not a function`). Added a `seedStore` platform shim in `mobile/src/lib/wallet.tsx`: localStorage on web (mirroring the web app's demo wallet), SecureStore on native. Native path unchanged. | `tsc --noEmit` clean · `npm test` 7/7 · **browser click-through on the web build (Playwright, 390×844)**: markets index → liquid ladder (47/43 spread) → connect → deposit $1,000 → buy 10 YES @ 45¢ → resting bid visible on ladder + portfolio row with Cancel, vault $995.50 — screenshots captured. On-device (Expo Go) pass still pending |
| 2026-07-16 | Prasad | **Mobile Task 12: lib-level e2e vs a live backend (final task).** Added `mobile/scripts/e2e-flow.ts` (`npm run e2e`, `tsx` devDep) — drives the shipped `mobile/src/lib/api.ts`/`borsh.ts` through wallet keygen → two-step/mirror deposit → find an open binary market → sign+place a resting bid via `borshOrder` → see it on `getBook`/`getPortfolio` → cancel it, asserting at each step. **No root `.env` exists on this machine** (no Neon `DATABASE_URL` anywhere — checked shell profiles, `~/.config`, git history; genuinely absent, not network flake), so `backend/cmd/server` couldn't start against Neon as the brief assumed. Ran it instead against a disposable local Postgres 16 (Docker, dropped after) with `DATABASE_URL` passed as a process env var only — **root `.env` was never touched, never created**. Same `DEMO_FIXTURE=demo-final` off-chain-mirror server everyone else uses; the store/exchange code path is identical to Neon, only the Postgres endpoint differs. `demo-final`'s replay fixture is only 15s of recorded match time, which at the default `REPLAY_SPEED=60` resolves every market before a script can even connect — set `REPLAY_SPEED=0.05` (a knob already in `cmd/server`, no code change) to hold markets open long enough. First run 401'd with "ed25519 signature does not verify" — root-caused to a **real bug**, not an environment issue: `randomSalt()` in `mobile/src/lib/borsh.ts` (ported from `frontend/lib/borsh.ts` in Task 2) clears only the top bit of a random uint64, but `api.postOrder`'s wire DTO sends `salt: Number(salt)` — `Number(bigint)` silently rounds any value ≥2^53, so ~999/1000 real orders sign one salt and transmit a different (rounded) one, and the backend's signature check on the reconstructed `Order` always fails. Fixed by narrowing `randomSalt()`'s range to stay under 2^53 (still clears the sign bit for the BIGINT column; comment explains why) and added a jest regression test (`bigint -> Number -> JSON -> uint64` round-trips exactly, 64 draws). **This same bug is present in `frontend/lib/borsh.ts` — untouched, out of scope for this task (mobile/ + progress.md only) — flagging here for a follow-up fix**, since the 07-15 "browser-flow e2e green" changelog entry likely got lucky on a small salt draw or predates the current `randomSalt()`. After the fix, `npm run e2e` ran green **3/3 consecutive times** against the live server. `npm test` 7/7 (1 new test), `npx tsc --noEmit` clean, `npm run check-borsh` ok. Server started/stopped manually (`go run ./cmd/server` in background, `lsof -ti:8080 \| xargs kill`); Postgres container `docker rm -f`'d after. **Step 3 (on-device click-through, the ✅ gate for the UI) was not run — no physical device in this environment** — every mobile UI flow (markets, market+ladder+trade, deposit, portfolio) stays 🟡 pending a human pass. | `mobile/scripts/e2e-flow.ts` green 3/3 vs `cmd/server` (off-chain mirror, local Postgres substituting for the unavailable Neon `.env`) · `npm test` 7/7 (new salt round-trip regression test) · `npx tsc --noEmit` clean · `npm run check-borsh` ok · on-device click-through **not run** — pending |
| 2026-07-16 | Prasad | **Mobile Task 11: Privy Expo embedded wallet behind `EXPO_PUBLIC_PRIVY_APP_ID`.** Extended `mobile/src/lib/wallet.tsx`'s `PitchWalletProvider` to pick a `PrivyProvider`/`PrivyBridge` backend when the env var is set (demo `SecureStore` wallet otherwise, unchanged, still the default) — mirrors `frontend/lib/wallet.tsx`'s selector. Checked current Privy Expo docs first (package list, hook names, message-signing shape all moved since the brief was written): `@privy-io/expo` v0.70.3 exports `PrivyProvider`, `usePrivy` (`{user, isReady, logout}`), `useLoginWithOAuth` (`login({provider})`), `useEmbeddedSolanaWallet` (`{wallets, status, create}`) — confirmed against the installed package's `.d.ts`, not just docs prose. **Deviation from the brief:** `connect()` uses `useLoginWithOAuth({provider:"google"})`, not the brief's sketched `useLogin({loginMethods:["email"]})` (that hook doesn't exist in `@privy-io/expo`) — the real Expo email flow is `useLoginWithEmail`'s two-call `sendCode`/`loginWithCode({code, email})`, which needs a form and doesn't fit `PitchWallet.connect(): Promise<void>`'s no-arg contract; OAuth is the one login hook that's a single call. `signMessage` base64-encodes the message (added `bytesToB64` to `mobile/src/lib/base64.ts`) because the installed SDK's `provider.request({method:"signMessage"})` takes `params.message: string` and always returns `{signature: string}` (base64) — never raw bytes as the brief's sketch guessed — decoded back via the existing `b64ToBytes`. `PitchWallet` mapping (ready/address/isDemo/connect/disconnect/signMessage→64-byte sig) kept byte-identical to what every screen already consumes. Installed `@privy-io/expo @privy-io/expo-native-extensions expo-application expo-crypto expo-linking expo-web-browser expo-apple-authentication react-native-webview react-native-passkeys expo-build-properties fast-text-encoding @ethersproject/shims` (the brief's install command omitted `expo-apple-authentication`/`react-native-passkeys`, which the OAuth chunk imports transitively — omitting them broke `expo export --platform web` with an unresolvable-module error; adding them fixed it). `mobile/app.json`: `com.pitchmarket.app` iOS bundle id / Android package, `expo-build-properties` plugin (`ios.deploymentTarget: "17.5"`), `expo-web-browser` plugin. `@privy-io/expo` is `require()`d lazily inside the `PRIVY_APP_ID` branch (not statically imported) as a defensive measure against it being pulled into Privy-less builds — though the actual web-export breakage traced to the missing peer deps above, not the import style; kept the lazy require anyway since it's free insurance and documents the risk inline. Added `EXPO_PUBLIC_PRIVY_APP_ID`/`EXPO_PUBLIC_PRIVY_CLIENT_ID` placeholders to `mobile/.env.example` (both blank — no real `.env` committed). **No Privy dashboard credentials and no physical device/dev-client build were available in this environment** — did not run `expo prebuild`/`expo run:ios`/`expo run:android`, per the brief's explicit no-device scope. | 🟡 **Privy path written but UNVERIFIED** — never run against a real Privy app, no dashboard app created, no OAuth login/wallet-creation/silent-signing exercised on a device. Demo path **regression-verified**: `npx tsc --noEmit` clean · `npm test` → 6/6 pass (all pre-existing, no new tests) · `npm run check-borsh` → golden vector ok · `npx expo export --platform web` succeeds **with no Privy env vars set** (7 static routes, same as before this task) — confirms the demo-wallet path and its bundle are untouched by the new Privy code path. |
| 2026-07-16 | Prasad | **Mobile Task 10: Portfolio screen — balance, positions, exit, open orders, cancel.** Replaced the Task 6 stub body of `mobile/src/app/(tabs)/portfolio.tsx` verbatim from the brief: vault balance + `DepositButton`, positions list with avg→current, unrealized PnL (`(current − avg_cost) × yes × 10_000` micro) and realized PnL when nonzero, an "Exit at {price}" button per position, open orders list with cancel. Exit signs a SELL YES order at `p.current` for the full `p.yes` shares — same borsh/postOrder field discipline as Task 8's TradeSheet (outcome 1, side 1, fee_bps 0, expiry 0, `salt: Number(salt)`, hex sig via `toHex`). No new libs needed — all consumed exports (`api.getPortfolio/cancelOrder/postOrder`, `usePitchWallet`, `DepositButton`, `borshOrder/fromHex/randomSalt/toHex`, `placeErrorMessage`, `cents/shares/usd`, `Portfolio`/`Position` types) already existed from Tasks 2–9. No deviations from the brief's code. | `npx tsc --noEmit` clean · `npm test` → 6/6 pass (no new tests, all pre-existing) · `npx expo export --platform web` succeeded (7 static routes incl. `/(tabs)/portfolio`) · on-device portfolio view (balance/positions/PnL matching web portfolio, cancel shrinking the ladder, exit filling against the bot's bid) against the live backend **not run** — pending device access |
| 2026-07-16 | Prasad | **Mobile Task 9: deposit flow** — real DepositButton (two-step cosigned deposit via depositInit → wallet.signMessage(message_b64 bytes) → depositComplete, mirror-faucet fallback on 409/null, 1,000 demo USDC) replacing the Task 8 stub. | tsc --noEmit clean · npm test 6/6 · expo export web ok · on-device deposit vs live backend pending |
| 2026-07-16 | Prasad | **Mobile Task 8: TradeSheet — borsh signing + order placement.** Added `mobile/src/lib/errors.ts` (`placeErrorMessage`, maps `ApiError` status → pinned copy: 0/401/402/409/410) and replaced the Task 7 stub body of `mobile/src/components/TradeSheet.tsx` verbatim from the brief — buy/sell toggle, price/size inputs, order-signing block byte-identical to web `TradePanel.place()` (outcome fixed to 1/YES ladder, `fee_bps: 0`, `expiry: 0` GTC, `salt: Number(randomSalt())`, hex sig via `toHex`), vault-balance/insufficient-funds guard, submit states (idle/signing/placed) with "Filled"/"Resting on book" label from `res.fills.length`. Props interface unchanged from the Task 7 stub. Added the brief's stub `mobile/src/components/DepositButton.tsx` (`return null`) so this compiles ahead of Task 9. No deviations from the brief's code. | `npx tsc --noEmit` clean · `npm test` → 6/6 pass (no new tests, all pre-existing) · `npx expo export --platform web` succeeded (7 static routes) · on-device trading against the live backend (connect → deposit → resting bid → crossing fill → 409 replay) **not run** — pending device access |
| 2026-07-16 | Prasad | **Mobile Task 7: market screen — hero, ladder, one-liners, fills.** Added `mobile/src/app/market/[id].tsx`, `mobile/src/components/MatchHeader.tsx`, `mobile/src/components/Ladder.tsx` verbatim from the brief (paths adjusted for the `src/` layout; `@/*` alias unchanged). Screen consumes `useLiveMarket` (Task 5) + `usePitchWallet` (Task 4): loading/error states, match header with live score/minute, big YES price with delta, rotating one-liner, top-5-bid/ask ladder with depth bars, last-8-fills list, and a bottom "Trade" button (disabled when market isn't open) that opens a `TradeSheet`. Added the brief's stub `mobile/src/components/TradeSheet.tsx` — exact props interface (`open`, `onClose`, `marketId`, `yesPrice`, `marketStatus`, `balanceMicro`, `onPlaced`), body `return null` — so the screen compiles until Task 8 replaces it. No deviations from the brief's code. | `npx tsc --noEmit` clean · `npm test` → 6/6 pass (no new tests, all pre-existing) · `npx expo export --platform web` succeeded, now emitting 7 static routes incl. new `/market/[id]` (18KB) · on-device tap-through, live WS book updates, one-liner rotation, and AppState foreground-refresh **not run** (no device available) — pending |
| 2026-07-16 | Prasad | **Mobile Task 6: tabs shell + live Markets index.** Added `mobile/src/app/(tabs)/_layout.tsx` (expo-router `Tabs`, Markets/Portfolio with `@expo/vector-icons` `Ionicons`), `mobile/src/app/(tabs)/index.tsx` (verbatim from the brief: polls `api.listMarkets("open")` + `api.listMatches()` every 20s, fetches `api.getBook` per binary market, computes YES mid, pull-to-refresh, taps navigate to `/market/[market_id]`), `mobile/src/app/(tabs)/portfolio.tsx` stub. Deleted the Task 1 placeholder `mobile/src/app/index.tsx`. `@expo/vector-icons` was **not** actually present in `node_modules` despite being an Expo-template staple — brief assumption was wrong for this scaffold; ran `npx expo install @expo/vector-icons` to add it as a real dependency (now in `package.json`). `tsconfig.json`'s `@/*` → `./src/*` alias already existed from Task 1 (no change needed). Added `mobile/.env` (`EXPO_PUBLIC_API_URL=http://localhost:8080`, matching the Go server's default `:8080` from `backend/cmd/server/main.go`) and committed `mobile/.env.example` with the same line plus a LAN-IP note for physical devices; tightened `mobile/.gitignore` (`.env*.local` alone didn't cover plain `.env`) to `.env` + `!.env.example`. | `npx tsc --noEmit` clean · `npm test` → 6/6 pass (no new tests) · `npx expo export --platform web` succeeded (6 static routes incl. `/(tabs)` and `/(tabs)/portfolio`) · `git check-ignore` confirms `.env` ignored / `.env.example` tracked · on-device markets list + live-backend fetch + pull-to-refresh + card-tap navigation **not run** — pending Task 7 |
| 2026-07-16 | Prasad | **Mobile Task 5: ported `useLiveMarket` with AppState foreground refetch.** Added `mobile/src/lib/useLiveMarket.ts` ported from `frontend/lib/useLiveMarket.ts` byte-for-byte except: dropped `"use client"`; added a `refreshKey` state + `AppState.addEventListener("change", ...)` listener that bumps `refreshKey` on transition to `active` (mobile OSes freeze JS/kill sockets in background, so the snapshot can be stale on return), included in both the initial-load effect's deps (`[marketId, wallet, refreshKey]`) and the WS effect's deps (`[marketId, state.loading, state.errorStatus, refreshKey]`). WS event handling (`book_update`/`fill`/`oneliner`/`match_state`), reconnect backoff, and one-liner rotation left untouched — RN's global `WebSocket` matches the web API surface used. No new jest test per the brief; behavioral verification (live WS on-device) deferred to Task 7. | `npx tsc --noEmit` clean · `npm test` → 6/6 pass (no new tests, all pre-existing) — on-device WS/AppState behavior **not yet run** |
| 2026-07-16 | Prasad | **Mobile Task 4: `PitchWallet` context — SecureStore demo wallet.** Added `mobile/src/lib/wallet.tsx` verbatim from the brief: `keypairFromSeed` (ed25519 via tweetnacl), `PitchWalletProvider`/`usePitchWallet` React context, demo backend persists a 32-byte seed in `expo-secure-store` (hex via existing `toHex`/`fromHex`), `connect`/`disconnect` async (unlike web, since SecureStore is async), `signMessage` returns `nacl.sign.detached`. Wrapped `mobile/src/app/_layout.tsx`'s `<Stack>` in `<PitchWalletProvider>`, imported via the `@/lib/wallet` alias; `import "react-native-get-random-values"` stayed the first line. TDD: wrote `mobile/src/lib/__tests__/wallet.test.ts` (brief's keypair sign/verify + bs58 round-trip test) first, confirmed it failed on missing `keypairFromSeed` export, then implemented. | `npm test` → 6/6 pass (1 new wallet test + 5 existing) · `npx tsc --noEmit` clean |
| 2026-07-16 | Prasad | **Mobile Task 3: ported `api.ts` (core endpoints only)** to `mobile/src/lib/api.ts` from `frontend/lib/api.ts`, byte-for-byte except: `EXPO_PUBLIC_API_URL` replaces `NEXT_PUBLIC_API_URL` (incl. both error-message strings); dropped `{ cache: "no-store" }` from `get()` (Next-ism, RN fetch doesn't cache); deleted out-of-scope `getSettlement`, `enterPrecision`, `leaderboard`, `createRFQ`, `getRFQ`, `acceptQuote` and the `PrecisionEntry`/`RFQQuote`/`RFQ` interfaces, removed `Settlement` from the type import. `mapMarket`, `mapMatch`, `mapBook` (YES-ladder unification), `mapFill`, `getPortfolio`'s title join/filters, `postOrder`/`cancelOrder`/the three deposit calls, `PROGRAM_ID`/`DEPLOY_TX`/`explorerTx`/`explorerAddr` untouched. TDD: wrote `mobile/src/lib/__tests__/api.test.ts` (brief's 2 `mapBook` tests) first, confirmed it failed on missing module, then ported. | `npm test` → 5/5 pass (2 new `mapBook` tests + 3 existing borsh tests) · `npx tsc --noEmit` clean |
| 2026-07-16 | Prasad | **Mobile Task 2: pure libs copied from web, borsh golden vector gated.** Copied `frontend/lib/{borsh,types,format}.ts` verbatim to `mobile/src/lib/` (alias `@/*` → `./src/*`, so `mobile/lib/` from the brief becomes `mobile/src/lib/`) and `frontend/scripts/check-borsh.mjs` verbatim to `mobile/scripts/`; wrote `mobile/src/lib/base64.ts` (`b64ToBytes`, Hermes-safe, no `atob`) and `mobile/src/lib/__tests__/borsh.test.ts` (3 tests: golden vector, salt top-bit, base64 decode). Added `tweetnacl`, `bs58`, `react-native-get-random-values`, `expo-secure-store` deps; `react-native-get-random-values` imported as the first line of `mobile/src/app/_layout.tsx` (before `global.css`) so `crypto.getRandomValues` exists for `randomSalt`/tweetnacl on Hermes. `npx expo install jest-expo jest @types/jest -- --save-dev` worked as-is (landed in `dependencies`, not `devDependencies` — expo install ignores the `--save-dev` flag; left as-is, doesn't affect test/build). Added `test`/`check-borsh` npm scripts + `jest: {preset: "jest-expo"}` to `mobile/package.json`; no transformIgnorePatterns tweak was needed. One deviation from the brief: `npx tsc --noEmit` failed with `Cannot find name 'test'/'expect'` even though `@types/jest` was installed (expo's base tsconfig sets no `types` array, and ambient jest globals weren't picked up under `moduleResolution: bundler`) — fixed by adding `"types": ["jest"]` to `mobile/tsconfig.json` compilerOptions; the three copied files and the check-borsh script were not touched. | `npm test` → 3/3 pass · `npm run check-borsh` → `borsh golden vector ok (94 bytes)` · `npx tsc --noEmit` clean · re-diffed all 4 copied files against `frontend/` post-change to confirm still byte-identical |
| 2026-07-16 | Prasad | **Mobile Task 1: scaffolded Expo app** at `mobile/` (`create-expo-app@4.0.0`, SDK 57, expo-router, TS strict) + NativeWind v4 with the exact web token palette (`bg`/`ink`/`muted`/`dim`/`line`/`line2`/`accent`/`down`) in `mobile/tailwind.config.js`. Template puts routes under `src/app/` (not `app/`) so `global.css`/content globs/metro `input` were adapted to that path; `nativewind-env.d.ts` also needed `declare module "*.css"` for TS6's stricter side-effect-import check. Minimal `src/app/_layout.tsx` (Stack, dark contentStyle) + `src/app/index.tsx` (bg/ink/accent classes) per the brief. `app.json` renamed to name `PitchMarket`, slug/scheme `pitchmarket`. | `npx tsc --noEmit` clean · `npx expo export --platform web` succeeded (bundler + NativeWind config load and compile) · device/Expo-Go boot **not** run — pending |
| 2026-07-16 | Prasad | **Mobile app design spec** (`docs/superpowers/specs/2026-07-16-mobile-app-design.md`): Expo/RN app at `mobile/`, core trading loop only (markets → trade → deposit → portfolio), Privy Expo embedded wallet (SecureStore demo-wallet as cut), copied `frontend/lib` pure-TS files gated by the borsh golden vector. No code yet. | doc only — n/a |
| 2026-07-16 | Ashish | **Resolution durability (reconciler)** — fixed the core fragility that resolution fired only on the ephemeral SSE `full_time` event, so a missed event (service down / stream drop / TxLINE never emits it) left a match stuck `live` forever (fixture `18241006`). Added event-driven-fast-path **+ reconciliation guarantee**: `txodds.FinalState` (folds the authoritative score snapshot → final score + finished), `store.UnresolvedMatches`, idempotent `ResolveFixture` (skips settled markets — safe re-runs, no double payout), and `cmd/server reconcileResolutions` (startup + 3-min ticker; hybrid policy: resolve on finished, else auto-resolve after 40-min score stability, else leave stale). Stale matches surfaced in `/admin/ops`. Modeled on the existing chain-index reconciler — not naive polling (idle when healthy). **Note: `total_passes` still unmapped from the feed → passes pool needs an admin value.** | `go build`/`vet` ✅ · `go test ./internal/lifecycle -run 'TestResolveFixtureIdempotent\|TestUnresolvedMatchesRecovery'` ✅ (idempotent re-resolve = no double payout; unresolved query surfaces + drops) · reconciler loop itself exercised only on restart (not yet observed recovering a live stuck match) |
| 2026-07-16 | Ashish | **Frontend cards + Yes/No trading** — Markets/Precision/Combos rendered as bordered card tiles (hover-lift, press feedback); binary cards carry Yes/No buttons that deep-link into the trade panel with the outcome preselected (`?o=yes\|no`); `TradePanel` made **outcome-aware** (YES/NO toggle, submits outcome 0/1 at its price — was YES-only). Fixed a card display bug where settled precision pools still showed "POOL" (now show the winning value / locked / pool via `PrecisionState`). Privy embedded wallet enabled (`NEXT_PUBLIC_PRIVY_APP_ID` in gitignored `frontend/.env`; secret NOT stored — server-side only). | `tsc --noEmit` ✅ · `npm run build` ✅ (all routes incl. `/admin`) · Privy app ID verified inlined into the client bundle · login click-test + Privy-dashboard Solana/origin config pending (user) |
| 2026-07-16 | Ashish | **Admin panel (`/admin`)** — operator-gated manual market control, the mitigation for the auto full-time cascade never firing on a quiet feed. Backend: `txodds.OddsSnapshot` on-demand accessor + `api.FixtureSource` wiring (provider lifted to `run()` scope, `WithFixtures`), `lifecycle.ResolveMarketManually` (single-market binary/precision resolve reusing the cascade primitives), new `internal/api/admin.go` — operator-wallet challenge→session auth (ed25519, in-memory token) + handlers for fixtures/odds/create-markets/resolve/close/clear-orders/resolve-from-score/ops. `ADMIN_PUBKEY` env (defaults to operator). Frontend: `app/admin/page.tsx` + `lib/adminApi.ts` reusing `usePitchWallet.signMessage`, design tokens, TopBar, VerifyLink. | `go build`/`vet` ✅ · `go test ./internal/api -run TestAdmin` ✅ (auth 401/403/200 + replay-reject; binary→settled, precision→settled, bad outcome→400, unauthed→401) · frontend `tsc --noEmit` ✅ · `npm run build` ✅ · live click-through pending |
| 2026-07-15 | Ashish | **Adversarial / demo-readiness pass** (`docs/DEMO-READINESS.md`). Found + fixed a real **uint64 overflow exploit**: a crafted order size wrapped `BuyCost` near zero, locking ~$0.25 for a 30-trillion-share order (accepted as 200) — now capped at `models.MaxOrderSize` in the engine + store. New tests: exchange invariants (concurrency no-over-fill, money conservation to the micro, replay-once, revert-reconcile), 28 HTTP attack-vectors (all clean 4xx, never 5xx), whole-server concurrent HTTP load (120 in-flight reqs, 0×5xx, WS stays live). Also fixed mirror-divergence (engine unwinds on rejected mirror write; bot quotes expire) and CreateIdempotent settle ATAs. | `go test ./internal/exchange` (chaos) ✅ · `./internal/api` (attack + load) ✅ · full regression green · live server restarted on the fixed binary, covering England vs Argentina |
| 2026-07-15 | Ashish | **Everything real, end to end.** TxLINE integration via the free World Cup tier (guest JWT → on-chain `subscribe` to txoracle `6pW64…` → activation; self-provisioning, cached) — England vs Argentina fixture live-priced (dnb 54¢). New TxLINE-priced templates (dnb_home w/ VOID, ou_1h_075) + per-market VOID resolution. Server on-chain mode: markets initialized/resolved on devnet, crank settles UI fills (CreateIdempotent ATA fix), two-step cosigned deposits, bot's own on-chain vault. Realized PnL on sells (+migration). Gemini one-liners. Frontend: fixtures.ts DELETED, markets index, combos RFQ builder (mutex greying, quote countdown), precision page (entry, distribution, leaderboard), portfolio exit/cancel + realized/unrealized PnL at best-bid mark, real deposit flow in TradePanel. | Scripted product e2e vs the LIVE stack: real deposit txs; fill → devnet settle `27VgrXKiLby34HiPorxTZYRBhK2R8zjn191sxK3MRtuADqJ3cBkHnaxV5txqHWFvpucq7ktMfAemXWydQj83M4qQ`; exit/cancel/precision ✓; combo quoted $5→$14.17 from live TxLINE prices, mutex rejected ✓ · Go suites + golden vector + `npm run build` ✅ · caveat: public devnet RPC 429s under load (index poll now 30s) |
| 2026-07-15 | Ashish | Frontend motion-polish pass (press feedback, state crossfades, sliding tab underline, scaleX depth bars, MotionConfig reduced-motion, touch-gated hovers); **wired frontend to the live exchange** (REST mapping + WS + Privy/demo wallet + real signing); CORS on the Go API; TS borsh encoder pinned to the golden vector in `npm run build` | `npm run build` ✅ (golden vector gate) · scripted browser-flow e2e vs `go run ./cmd/server` ✅: deposit → signed order accepted → bad sig 401 → replay 409 → ladder shows the bid → portfolio row |
| 2026-07-15 | Ashish | **Program deployed to devnet** at the pinned ID; **Go crank settled a real match on devnet** (v0+ALT); gentler RPC polling in crank/harness (public devnet endpoint rate-limits) | `cmd/devnet-e2e` ✅ full run: initialize→vaults→deposits→engine MINT fill→**settle_match**→resolve→redeem, every balance asserted; tx sigs in §5 |
| 2026-07-12 | Ashish | **Crank v0 + per-market ALT** (`crank/lut.go`, `BuildSettleMatchTxV0`); chain builders (initialize_market/init_vault/deposit/resolve/redeem) + `cmd/devnet-e2e` harness; committed program keypair (decision #1 closed); reproduced §4 fix on 2nd machine (Agave 4.1.1 → `.so` 419,400 B); pinned v0+ALT in interface-contract §6.5. **Devnet deploy blocked only on faucet SOL.** | `go test ./internal/crank` ✅ (v0 1116 B ≤ 1232, legacy 1421 B rejected; layout tests) · `cargo-build-sbf` ✅ on this machine · devnet run pending funds |
| 2026-07-12 | Ashish | Merged PR #3 into main; reconciled this file across both tracks (E1 localnet results + E2 backend state + v0/ALT crank rework now tracked in §5/§7) | host `cargo test -p pitchmarket` ✅ · `go build`/`vet` + targeted Go suites ✅ on the merged tree |
| 2026-07-13 | E1 | Added MERGE + cancel_order tests; refactored the TS harness into `tests/helpers.ts` (single borsh impl) | `npm test` **8/8 ✅** on `solana-test-validator` — all settle paths + cancel fail-closed |
| 2026-07-12 | E1 | Fixed §4 build blocker (platform-tools v1.54); Boxed `SettleMatch` accounts (BPF stack overflow); added `idl-build` feature; added TS lifecycle test harness (`tests/`, `package.json`) | `cargo build-sbf` ✅ · `npm test` 5/5 ✅ on `solana-test-validator` (initialize→deposit→settle MINT+NORMAL→resolve→redeem, balances asserted) |
| 2026-07-11 | Ashish | **E2 backend complete**: matching MINT/MERGE + cancel/expiry/unfill; Postgres store on Neon; exchange core with revert→reconcile; crank §6.5 tx builder + RPC submitter + off-chain mode; WS hub; full REST API; RFQ with mutex groups; precision; MM bot; lifecycle; txodds SSE skeleton; Claude one-liners; chain-index poller; server wiring; borsh golden vectors Go↔Rust; demo replay fixture | `go build` ✅ `go vet` ✅ · every test package green against real Postgres (11 pkgs incl. HTTP e2e + WS + revert path); two packages hit Neon network timeouts in the one full serialized run and passed on immediate re-run (§3 caveat) · `cargo test -p pitchmarket` ✅ 4/4 · `anchor build` ❌ at the time (fixed next day, §4) |
| 2026-07-10 | Ashish | Added `progress.md` + `CLAUDE.md`; trimmed stale README status; untracked `.DS_Store`; committed the E1/E2 scaffold | `cargo check` ✅ · `go build ./... && go vet ./...` ✅ · `anchor build` ❌ (§4) |
| 2026-07-09 | E1 | Implemented `sig_verify::verify_order_signature`; pinned settle_match tx layout in interface-contract §6.5 | `cargo check` only — never executed |
| 2026-07-08 | E1/E2 | Anchor program scaffold; Go matching engine, crank skeleton, order API, replay feed, Postgres schema | `cargo check` · `go build` |
