# Leave unset to run fully fixture-driven (no backend needed).
# Set to the Go backend's base URL to render against live data:
#   NEXT_PUBLIC_API_URL=http://localhost:8080
# The backend REST mux needs CORS enabled for this origin (see HANDOFF punch list).
NEXT_PUBLIC_API_URL=
